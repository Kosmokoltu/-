# Полная инструкция по созданию репозитория на GitHub

## 📦 Финальная структура проекта

```
HockeyTodoApp/
├── .github/
│   └── workflows/
│       └── dotnet.yml              # GitHub Actions (опционально)
├── Models/
│   ├── ToDoUser.cs
│   ├── ToDoItem.cs
│   └── ToDoItemState.cs
├── Exceptions/
│   ├── TaskCountLimitException.cs
│   ├── TaskLengthLimitException.cs
│   ├── DuplicateTaskException.cs
│   └── TaskNotFoundException.cs
├── Program.cs                      # Главный файл
├── HockeyTodoApp.csproj           # Конфигурация проекта
├── .gitignore                      # Исключения для Git
├── README.md                       # Основная документация
└── SETUP.md                        # Эта инструкция
```

## 🚀 Быстрый старт (5 минут)

### Шаг 1: Скопируйте все файлы локально

Создайте эту структуру на вашем компьютере:

```
C:\Projects\HockeyTodoApp\        (Windows)
/Users/username/HockeyTodoApp/    (macOS)
/home/username/HockeyTodoApp/     (Linux)
```

Скопируйте в эту папку все файлы согласно структуре выше.

### Шаг 2: Инициализируйте Git локально

**Windows (PowerShell):**
```powershell
cd C:\Projects\HockeyTodoApp
git init
git add .
git commit -m "Initial commit: HockeyTodoApp v2.0"
```

**macOS/Linux (Terminal):**
```bash
cd ~/HockeyTodoApp
git init
git add .
git commit -m "Initial commit: HockeyTodoApp v2.0"
```

### Шаг 3: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Введите:
   - **Repository name:** `HockeyTodoApp`
   - **Description:** `Console application for task management`
   - **Visibility:** `Public`
3. **НЕ** проверяйте "Add a README file" или ".gitignore" (у вас уже есть!)
4. Нажмите **"Create repository"**

### Шаг 4: Загрузите на GitHub

Копируйте команды из GitHub (они будут показаны после создания репо):

```bash
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/HockeyTodoApp.git
git push -u origin main
```

**Готово! 🎉**

---

## 🔍 Проверка после загрузки

Откройте https://github.com/YOUR_USERNAME/HockeyTodoApp и проверьте:

- ✅ Все файлы видны в браузере
- ✅ Папки Models/ и Exceptions/ отображаются
- ✅ README.md показывается на главной странице
- ✅ Зелёный значок "✓" рядом с коммитом

---

## 💡 Как скачать проект другие люди

Теперь любой может клонировать ваш проект:

```bash
git clone https://github.com/YOUR_USERNAME/HockeyTodoApp.git
cd HockeyTodoApp
dotnet run
```

---

## 🛠 Дополнительные опции

### Вариант 1: Автоматическая сборка (GitHub Actions)

Создайте файл `.github/workflows/dotnet.yml`:

```yaml
name: .NET Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'
    
    - name: Restore dependencies
      run: dotnet restore
    
    - name: Build
      run: dotnet build --no-restore
    
    - name: Test (if you have tests)
      run: dotnet test --no-build --verbosity normal || true
```

После этого проект будет автоматически собираться при каждом push!

### Вариант 2: Создание релиза

1. Перейдите на страницу репо
2. Нажмите **"Releases"** в боковой панели
3. Нажмите **"Create a new release"**
4. Заполните:
   - **Tag version:** `v2.0.0`
   - **Release title:** `Hockey Todo App v2.0.0`
   - **Description:** Опишите новые фичи
5. Нажмите **"Publish release"**

### Вариант 3: Добавьте скрин приложения

1. Запустите приложение
2. Сделайте скриншот терминала
3. Добавьте в README.md:

```markdown
## 📸 Скриншот

![App Screenshot](screenshot.png)
```

Загрузите `screenshot.png` в репо.

---

## 🔐 Рекомендации

### Обновление проекта

Когда вы что-то изменили:

```bash
# Проверьте изменения
git status

# Добавьте все изменения
git add .

# Создайте коммит с описанием
git commit -m "Fix: improved task validation"

# Загрузите
git push
```

### Хорошие сообщения коммитов

```bash
git commit -m "Add: new /completetask command"           # Новая фича
git commit -m "Fix: bug in task deletion"                # Исправление
git commit -m "Refactor: restructure code into modules" # Рефакторинг
git commit -m "Docs: update README with examples"       # Документация
git commit -m "Perf: optimize task searching"           # Оптимизация
```

---

## ❓ Часто задаваемые вопросы

### Q: Где мой репо после создания?
**A:** Откройте https://github.com/YOUR_USERNAME/HockeyTodoApp

### Q: Как пригласить друзей?
**A:** 
1. Перейдите на страницу репо
2. Нажмите **Settings** → **Collaborators**
3. Добавьте их GitHub username

### Q: Как создать веток для фич?
```bash
git checkout -b feature/new-feature
# ... делайте изменения ...
git push origin feature/new-feature
# Откройте Pull Request на GitHub
```

### Q: Как откатить изменение?
```bash
# Посмотрите историю
git log --oneline

# Вернитесь на старый коммит
git revert COMMIT_HASH
git push
```

### Q: Как удалить репо?
1. Перейдите **Settings** репо
2. Прокрутите в конец
3. Нажмите **"Delete this repository"**

---

## 🎓 Полезные команды Git

```bash
# Проверить статус
git status

# Просмотр истории
git log --oneline

# Просмотр разветвлений
git branch -a

# Синхронизация с remote
git pull

# Отправить локальные изменения
git push

# Откатить последний коммит (без удаления файлов)
git reset --soft HEAD~1

# Просмотр отличий
git diff

# Создать новую ветку и перейти на неё
git checkout -b branch-name
```

---

## 📞 Поддержка

Если возникли проблемы:

1. Прочитайте это руководство ещё раз
2. Посмотрите документацию GitHub: https://docs.github.com
3. Посетите Stack Overflow с тегом `github`
4. Создайте Issue в репо (если вы создатель)

---

## ✅ Чеклист завершения

- [ ] Создана локальная папка проекта
- [ ] Скопированы все файлы по структуре
- [ ] Инициализирован Git (`git init`)
- [ ] Создан первый коммит (`git commit`)
- [ ] Создан репо на GitHub
- [ ] Добавлен remote URL (`git remote add origin`)
- [ ] Загружены файлы (`git push`)
- [ ] Проверено, что все видно на GitHub
- [ ] Проверено, что другие могут клонировать

**Поздравляем! 🎉 Ваш проект готов к использованию!**
